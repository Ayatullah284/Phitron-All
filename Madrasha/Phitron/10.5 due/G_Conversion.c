#include<stdio.h>
#include<string.h>
int main(){


    char a[100001];
    scanf("%s", &a);
    // 97 65 32
    // printf("%s", a);
    int length = strlen(a);

    for (int i = 0; i < length; i++)
    {
        
    
    }
    printf("%s\n", a);

   


    return 0;
}