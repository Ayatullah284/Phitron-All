#include<stdio.h>
#include<string.h>
int main(){


    int n;
    scanf("%d",&n);
    // printf("%d\n",n);
    int T[1000];
    for (int i = 0; i < strlen(T); i++)
    {
        scanf("%d",&T[i]);
        printf("%d ",T[i]);

    }

    return 0;
}